// Populated in Phase 2 with the season/matchday/team calendar seed data.
// Left as a no-op stub during scaffolding so `pnpm prisma:seed` has something to run.
async function main() {
  console.log("No seed data yet — this is filled in during Phase 2 (League CRUD + Membership).");
}

main();
